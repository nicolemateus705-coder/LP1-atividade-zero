#include <iostream>
#include <string>
using namespace std;

int main() {
    string texto;

    cout << "Digite uma frase: ";
    getline(cin, texto);

    cout << "A frase possui " << texto.length() << " caracteres." << endl;

    return 0;
}
